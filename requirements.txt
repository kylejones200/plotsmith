# Pinned dependencies for CI and reproducible builds
# Generated for Python 3.12+

# Core dependencies
matplotlib>=3.5.0,<4.0.0
numpy>=1.20.0,<3.0.0
pandas>=1.3.0,<3.0.0

# Development dependencies (install with: pip install -r requirements.txt)
# For dev dependencies, use: pip install -e ".[dev]"

